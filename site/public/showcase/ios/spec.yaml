# Showcase demo spec — schema per task #782 (record_demo.py is the consumer).
# placeholder: true until the pipeline records the real demo.
placeholder: true
demo: ios
title: iPhone Support
category: platform
featured: true
docs: /features/ios/
duration_target_s: 60
device: ios
platform_notes: "Appium + WebDriverAgent; QuickTime mirror for recording."
setup:
  - step: wake_unlock
# Timeline is CAPTIONS/timing only for --compose (the terminal comes from an
# external cast; these action cmds are not executed). ios-tester/adb-marketing
# to finalize captions to match the real hero cast.
timeline:
  - t: 0
    caption: "Same agent, now with an iPhone attached"
    action: terminal_exec
    args: { cmd: "true" }
  - t: 10
    caption: "Drive iOS with the same tool calls"
    action: terminal_exec
    args: { cmd: "true" }
  - t: 20
    caption: "Real phones. Any AI. Zero cloud."
    action: show_phone
outro:
  cta_line: "pip install ghost-in-the-droid"
  cta_link: /getting-started/installation/
highlight_window: { start_s: 14, end_s: 22 }
